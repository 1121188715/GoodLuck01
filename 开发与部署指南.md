# GoodLuck 开发与部署指南

本文档汇总了代码上传、部署流程，以及项目修改总结，便于在对话丢失后快速恢复项目和思考过程。

**当前版本**：v1.0.2.0（2025-02-28）

---

## 一、上传与部署流程

### 1. 本地修改后推送到 Git

在项目根目录执行：

```bash
cd C:\Users\11211\GoodLuck   # 或你的实际路径

git add .
git commit -m "你的修改说明"
git push origin main
```

> 若主分支名为 `master`，将 `main` 改为 `master`。

### 2. 服务器拉取并重启

SSH 登录服务器后执行：

```bash
cd /var/www/GoodLuck

git pull origin main

# 若有新增 Python 依赖
# cd backend && .venv/bin/pip install -r requirements.txt

systemctl restart goodluck
```

### 3. 首次在服务器配置 Git 时（仅一次）

若服务器项目目录尚未关联远程仓库：

```bash
cd /var/www/GoodLuck

# 解决「dubious ownership」报错
git config --global --add safe.directory /var/www/GoodLuck

# 添加远程（若显示 origin already exists，说明已添加，跳过此步）
git remote add origin https://github.com/1121188715/GoodLuck01.git

# 拉取代码
git pull origin main
```

若提示 `remote origin already exists`：说明 origin 已存在，直接执行 `git pull origin main` 即可。

---

## 二、今日内容总结

### 1. 本地数据库（修改仅影响本机）

- **行为**：用户点击「修改数据库」后编辑并保存，数据写入浏览器 `localStorage`，不再上传到服务器。
- **效果**：不同设备/浏览器各自使用自己的本地数据，互不影响。

**相关文件：**

| 文件 | 作用 |
|------|------|
| `frontend/js/database.js` | `loadFromLocal()` / `saveToLocal()`，保存时仅写 localStorage |
| `frontend/js/game.js` | `getLocalContent()` 从 localStorage 读取，创建游戏时附带 `custom_punishments`、`custom_fate_items` |
| `backend/app/schemas/game.py` | GameCreate 接收 `custom_punishments`、`custom_fate_items` |
| `backend/app/services/game_service.py` | 将自定义内容写入 `game.events`，`_get_custom_content()` 解析 |
| `backend/app/services/cell_content_service.py` | 优先使用 `custom_punishments` 生成格子内容 |
| `backend/app/services/cell_effect.py` | 优先使用 `custom_fate_items` 作为命运格文本 |

### 2. 版本标记

- **位置**：页面左下角固定显示（当前 `v1.0.1.0`）
- **用途**：确认服务器部署的是否为新版本
- **修改**：编辑 `frontend/index.html` 中 `#appVersion` 的文本，同步更新版本号

### 3. 本局使用本地内容提示

- **行为**：当本局使用了本地自定义惩罚/命运时，侧边栏显示「本局使用本地自定义内容」
- **作用**：便于在双设备测试时确认本地数据是否生效

### 4. 验证本地数据是否生效

1. **设备 A**：修改数据库 → 保存 → 开始游戏 → 应看到「本局使用本地自定义内容」，格子/命运为自定义内容
2. **设备 B**：不修改、不保存，直接开始游戏 → 不应看到该提示，内容为服务器默认

### 5. 恢复服务器默认

若想清空本地自定义数据，使用服务器默认：

```javascript
// 在浏览器控制台执行
localStorage.removeItem("goodluck_local_content");
// 然后刷新页面
```

---

## 三、项目结构速览

```
GoodLuck/
├── backend/                    # 后端 FastAPI + SQLite
│   ├── app/
│   │   ├── api/               # 路由：games, boards, database
│   │   ├── models/            # 数据模型
│   │   ├── schemas/           # Pydantic 请求/响应
│   │   └── services/          # 业务逻辑
│   │       ├── game_service.py      # 对局、掷骰子、格子构建
│   │       ├── cell_content_service.py  # 格子内容生成
│   │       └── cell_effect.py        # 格子效果（命运、前进/后退等）
│   └── requirements.txt
├── frontend/
│   ├── index.html             # 主页面、版本号
│   ├── css/style.css          # 样式、版本号样式
│   └── js/
│       ├── app.js             # API 封装、API_BASE
│       ├── game.js            # 游戏逻辑、创建对局、掷骰子
│       ├── board.js           # 棋盘渲染
│       └── database.js        # 修改数据库弹窗、本地存储
├── deploy/
│   ├── deploy.sh              # 首次一键部署
│   ├── nginx.conf
│   └── goodluck.service
├── 开发与部署指南.md           # 本文档（部署与项目速览）
└── CHANGELOG.md               # 版本更新日志（含 v1.0.1.0 详细变更）
```

---

## 四、常用操作速查

| 操作 | 命令 / 位置 |
|------|-------------|
| 本地提交并推送 | `git add .` → `git commit -m "..."` → `git push origin main` |
| 服务器拉取 | `cd /var/www/GoodLuck` → `git pull origin main` |
| 重启后端服务 | `systemctl restart goodluck` |
| 查看后端状态 | `systemctl status goodluck` |
| 查看后端日志 | `journalctl -u goodluck -f` |
| 修改版本号 | `frontend/index.html` 中 `#appVersion` |
| 修改 API 地址 | `frontend/js/app.js` 中 `window.API_BASE` |

---

## 五、v1.0.1.0 功能与思考过程速览

### 挑战格（语音数 1-20）

- **设计**：落子到挑战格 → 调用麦克风 → 语音转文字 → 校验是否从 1 数到 20 → 成功无惩罚，失败退 2 格
- **校验要点**：数字不缺不少、顺序正确、多余字符≤5；支持阿拉伯/中文数字；连续「123456789」需拆为 1-9
- **关键点**：`rec.stop()` 后需等 `rec.onend` 再校验，否则转写可能未完成

### 链式效果

- **设计**：前进/后退后若新格仍有特殊效果（如退回起点），继续触发
- **实现要点**：用 `pos_by_pos = {c.position: c for c in board.cells}` 按位置查找，避免 `next()` 遍历问题；循环直到无位置变化
- **调试**：`chain_triggered`、`effect_chain_count` 用于确认链式是否触发

### 文档与版本

- 详细变更见 `CHANGELOG.md`
- 每次发版更新 `frontend/index.html` 中的 `#appVersion`

---

## 六、Vosk 语音模型（挑战格离线识别）

挑战格优先使用 **Vosk 离线识别**，改善移动端体验；模型未部署或加载失败时自动回退到 Web Speech API。

### 模型部署步骤

1. 下载中文模型：https://alphacephei.com/vosk/models → `vosk-model-small-cn-0.22.zip`（约 42MB）
2. 解压后得到 `vosk-model-small-cn-0.22` 文件夹
3. 打包为 tar.gz：
   ```bash
   mv vosk-model-small-cn-0.22 model
   tar czvf model.tar.gz model
   ```
4. 将 `model.tar.gz` 放入 `frontend/models/`，即 `frontend/models/model.tar.gz`
5. 确保 Nginx `root` 为 `frontend` 目录，请求 `/models/model.tar.gz` 可访问

### 自定义模型地址

在 `index.html` 中、或任意在 `game.js` 之前执行的脚本中设置：

```javascript
window.VOSK_MODEL_URL = "/models/model.tar.gz";  // 默认值
```

### 相关文件

| 文件 | 作用 |
|------|------|
| `frontend/js/vosk-speech.js` | Vosk 封装：加载模型、录音、识别 |
| `frontend/js/game.js` | 挑战格弹窗：优先 Vosk，失败回退 Web Speech API |
| `frontend/models/README.md` | 模型下载与打包说明 |

---

## 七、后续续改建议

- 每次修改后执行：**本地 push → 服务器 pull → `systemctl restart goodluck`**
- 通过页面左下角版本号确认部署
- 涉及数据库结构或依赖变更时，记得在服务器执行 `pip install -r requirements.txt`
